---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: node-webapp
spec:
  type: ClusterIP
  selector:
    app: node-webapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
